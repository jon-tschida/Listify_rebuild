import { list } from 'postcss'
import React from 'react'

export default function GroceryList(props) {

    const {listIngredients} = props
  return (
    <div>
    {/* {listItems.map((el) => <p>{el}</p>)} */}
    </div>
  )
}
